# dsh-neo-skin roster patch — merged by `dsh plugin add` (dsh.bundle.patch).
# Mounts the host half so the client bundle reaches window.__DSH_BOOT__.
- insert:
    - id: dsh-neo-skin
      name: 'dsh-neo-skin'
